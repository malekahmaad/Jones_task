# Jones_task

Setup for task1:

Run these commands in the terminal:

1- Install the task dependencies: npm install

2- Install Playwright browsers: npx playwright install

3- Run the tests: npx playwright test
